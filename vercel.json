{
  "version": 2,
  "buildCommand": "cd backtest-dashboard && npm install && npm run build",
  "outputDirectory": "backtest-dashboard/.next",
  "installCommand": "cd backtest-dashboard && npm install",
  "framework": "nextjs",
  "rootDirectory": "backtest-dashboard"
}
