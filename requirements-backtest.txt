# Minimal deps for Phoenix orchestrator backtests (use with: python3 -m venv .venv && .venv/bin/pip install -r requirements-backtest.txt)
pandas>=2.0
numpy>=1.23
requests>=2.28
yfinance>=0.2.36
langgraph>=0.2
openpyxl>=3.1
